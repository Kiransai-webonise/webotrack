

<%= form_with model: @webouser, url: assign_asset_update_path, method: :put do |form| %>
  <% if @webouser.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@webouser.errors.count, "error") %> prohibited this webouser from being saved:</h2>

      <ul>
        <% @webouser.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.collection_select :asset_id, @assets, :id, :name, prompt: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>